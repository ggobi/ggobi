/* 
= ASH plot =

The ASH plot is a fast smooth histogram for 1d data (see also texturing).

This stage takes a single variable as input, and produces a new variable which
represents the estimated 1d density at each point of the input.

*/

public class GGobi.StageAsh : Stage {
	
}